import { app } from './app'
/**********************************************************/
const port = 7000

app.listen(port, () => {
    console.log(`Example app listening on port ${port}`)
})

module.exports = server;